overwrite: true
schema: "../../../schema.graphql"
documents: "src/operations/**/*.graphql"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "urql-introspection"
      - typescript-urql
