query GetFullApp($name: String!) {
  getApp(name: $name) {
    name
    reviewApp
    cloudSourceRepo
    githubURL
    slackChannel
    paused
    imageTag

    vars {
      key
      value
    }

    env {
      key
      value
    }

    reviewApps {
      name
    }

    excludedResources {
      name
      kind
    }
  }
}
