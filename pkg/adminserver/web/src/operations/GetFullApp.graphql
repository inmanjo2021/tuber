query GetFullApp($name: String!) {
  getApp(name: $name) {
    name
    reviewApp

    vars {
      key
      value
    }

    env {
      key
      value
    }

    reviewApps {
      name
    }
  }
}
