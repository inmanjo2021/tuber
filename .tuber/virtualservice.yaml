apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: 'tuber-ingress'
spec:
  hosts:
  - {{.clusterDefaultHost}}
  gateways:
  - {{.clusterDefaultGateway}}
  http:
  - match:
    - uri:
        prefix: /proto.Tuber
    route:
    - destination:
        host: tuber
